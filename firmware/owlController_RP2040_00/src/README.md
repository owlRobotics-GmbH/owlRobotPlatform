# owlController src

Arduino compiles every `.cpp` below the sketch `src/` directory recursively.
For that reason, third-party Arduino libraries are not stored here. They are
provided in `../libraries/` and used by setting the Arduino sketchbook path to
the owlController directory.
